<%= render(@cart.line_items) %>
<div class="container">
	<div class="row">
		<div class="col-sm-12 col-md-12 col-md-offset-1">
      <table class="table table-hover">
        <tbody>
            <tr>
              <td></td>
              <td></td>
              <td class="col-sm-1 col-md-1 text-right"><h4><strong>Total</strong></h4></td>
              <td></td>
              <td class="text-right"><h4><strong><%= number_to_currency(@cart.total_price) %></strong></h4></td>
            </tr>
        </tbody>
      </table>
		</div>
	</div>
</div> 
<div class="container">
  <div class="row">
		<div class="col">
		</div>
		<div class="btn-group col mb-4" role="group" aria-label="Basic mixed styles example">
				<%= link_to 'Empty cart', @cart, method: :delete, data: { confirm: "Are you sure?" }, class: "btn btn-danger" %>
				<%= link_to 'Keep shopping', root_path, class: "btn btn-warning" %>
				<%= link_to 'Checkout', new_order_path, class: "btn btn-success" %>	
		</div>
	</div>
</div>