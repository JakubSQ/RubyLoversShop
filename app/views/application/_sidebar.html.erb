<h3 class="my-4">Categories</h3>
<div class="list-group">
  <% @categories.each do |category| %>
  <%= link_to category.title.capitalize, products_path(q: {category_title_eq: category.title, commit: "Search"}), class: "list-group-item" %>
  <% end %>
</div>
