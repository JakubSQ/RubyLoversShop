<nav class="navbar navbar-expand-lg navbar-dark bg-dark mb-3">
  <div class="container">
    <a class="navbar-brand" href="#">Ruby Lovers</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarResponsive" aria-controls="navbarResponsive" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarResponsive">
      <ul class="navbar-nav">
        <li class="nav-item <%= active_class('/') %>">
          <%= link_to "Home", root_path, class: "nav-link" %>
            <span class="sr-only">(current)</span>
          </a>
        </li>
      </ul>
      <ul class="navbar-nav ml-auto">
        <% if !user_signed_in? %>
          <% if total_cart_items %>
          <li class="nav-item <%= active_class("/carts/#{session[:cart_id]}") %>">
            <%= link_to cart_path(@cart), class: "nav-link btn btn-outline-secondary btn-inverse" do %>
              Cart <span class="badge badge-light"><%= cart_count_over_one %></span>
            <% end %>
          </li>
          <% end %>
          <li class="nav-item active">
            <%= link_to "Log in", new_user_session_path, class: "nav-link btn btn-outline-secondary btn-inverse" %>
          </li>
          <li class="nav-item active">
            <%= link_to "Sign up", new_user_registration_path, class: "nav-link btn btn-outline-secondary btn-inverse" %>
          </li>
        <% else %>
          <% if total_cart_items %>
          <li class="nav-item <%= active_class("/carts/#{session[:cart_id]}") %>">
            <%= link_to cart_path(@cart), class: "nav-link btn btn-outline-secondary btn-inverse" do %>
              Cart <span class="badge badge-light"><%= cart_count_over_one %></span>
            <% end %>
          </li>
          <% end %>
          <li class="nav-item active">
            <%= link_to "Log out", destroy_user_session_path, method: :delete, class: "nav-link btn btn-outline-secondary btn-inverse" %>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
